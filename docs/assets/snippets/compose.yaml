services:
  anibridge:
    image: ghcr.io/anibridge/anibridge:v2
    environment:
      PUID: 1000
      PGID: 1000
      UMASK: 022
      TZ: "Etc/UTC"
    volumes:
      - /path/to/anibridge/data:/config
    ports:
      - 4848:4848
    # healthcheck:
    #   # Both `/livez` and `/readyz` endpoints are available
    #   test: ["CMD", "wget", "-q", "--spider", "http://localhost:4848/livez"]
    restart: unless-stopped
